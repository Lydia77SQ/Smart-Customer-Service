# API 相对路径（禁止写成完整后端 URL，避免触发 CORS）
VITE_API_BASE_URL=/api

# Vite 开发代理目标；Agent/Tester 默认 8099，用户门禁改为 http://localhost:8003
VITE_BACKEND_PROXY_TARGET=http://localhost:8099

# Axios 超时（毫秒）
VITE_AXIOS_TIMEOUT_MS=30000

# true 走 frontend/src/mocks；对接真实后端时设为 false
VITE_USE_MOCK=true
